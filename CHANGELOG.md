# 更新日志 / Changelog

## [1.0.6] - 2026-06-18

### 修复问题

- **Win 端日历 tooltip 不自动消失**：`render()` 开头调用 `removeTooltip()`，防止光标同步触发重渲染时销毁格子导致 `mouseleave` 不触发；同时在 `onOpen()` 注册 `scroll` 捕获事件清除 tooltip
- **tooltip 被窗口边缘截断**：`max-width` 从 260px 扩展到 320px；`showTooltip()` 增加视口边界检测，先隐藏挂载测量尺寸后再修正位置（右侧溢出翻转到左侧、下方溢出翻转到上方）
- **手机端长按预览分隔线混乱**：`getDayPreview` 新增 `isSeparatorLine()` 过滤，连续 10+ 个 `-` 的行不显示在预览中；移动端 Notice 预览每行截断至 24 字

---

## [1.0.5] - 2026-06-12

### 新增功能

- **中国法定节假日标注**：日历和文件日期标题自动标注节假日名称（元旦、春节、清明、劳动节、端午、中秋、国庆）
- **联网自动获取节假日**：启动时自动从 NateScarlet/holiday-cn 获取最新节假日数据，失败回退内置数据（覆盖 2026-2027）
- **添加工作记录方式可设置**：支持「选择上午/下午」和「插入当前时间」两种模式
- **上午/下午分隔线样式**：改为 `----------------------------上午-------------------------------` 格式，插入后光标自动定位到下一行
- **时间戳模式优化**：
  - 选中今天：跳转并插入当前时间
  - 选中非今天：跳转到该日期并定位到内容末尾
- **未来日期点击行为**：每天新增模式下点击未来日期跳转到今天，全年模式下正常跳转

### 优化改进

- **日历光标同步**：光标在日期标题下的内容行时，日历也能自动跳转并选中对应日期
- **手动选日期保护**：手动选中日期后 3 秒内忽略编辑器光标同步，防止按钮操作跳到错误日期
- **日期重建**：`repairYearStructure` 和 `trimAfterToday` 重写为全量重建方式，彻底解决日期错乱、内容被挤、周标题分离等问题
- **单日期插入**：点击未来缺失的日期只插入该日期，不会插入整周整月

### 修复问题

- 修复启动时日历视图重复出现两个的问题
- 修复 insertDayHeading 插入位置计算错误导致工作内容被挤到后续日期
- 修复全年→每天模式切换后月份标题残留的问题
- 修复点击未来日期时整周日期被挤入导致内容错乱的问题

---

## [1.0.4] - 2026-06-11

### 新增功能

- **待办功能**：新增"添加待办"按钮，在光标处插入可勾选的待办项 `- [ ]`
- **待办面板**：日历底部常驻显示所有未完成的待办列表，点击可跳转到对应文件的行
- **日期格式可配置**：设置中支持选择日期标题格式（YYYY-MM-DD、MM-DD、M月D日、YYYY年MM月DD日）
- **一键迁移**：设置中新增"应用新日期格式"按钮，批量将旧格式日期标题转换为新格式
- **编辑器光标同步**：编辑器光标移动到某日期标题行时，日历自动跳转并选中对应日期
- **日期生成模式**：设置中可选择日期生成模式
  - `每天新增到当前日`（默认）：只生成到今天，保持文件简洁
  - `每年自动生成全年`：一次性生成全年日期结构
  - 模式切换时自动处理：全年→每天会裁剪未来日期；每天→全年会补全到年底
- **日期圆点标记**：有工作记录的日期下方显示圆点标记
- **待办圆点标记**：有未完成待办的日期显示红色圆点标记（显示待办数量）

### 优化改进

- **交互改进**：去掉右键菜单，改为左键单击日期打开对应日期内容
- **添加记录交互**：点击"添加工作记录"按钮后弹出选项，选择"上午"或"下午"
- **Hover 预览**：鼠标悬停日期时显示临时选中框，底部按钮文字动态变化
- **添加记录去重**：点击"上午"/"下午"时先检查是否已存在，已存在则光标跳到该行末尾，不存在则正常插入
- **日历位置**：移除设置项，硬编码为右侧边栏显示
- **日期格式兼容**：改日期格式后，旧格式日期标题仍能正常解析（多格式回退解析）
- **滚动定位优化**：改进滚动定位逻辑，多次重试确保大文件编辑器就绪

### 修复问题

- 修复 Win 端日历只显示"1"的问题（`this.isMobile()` → `this.app.isMobile`）
- 修复待办不可点选的问题（确保 `- [ ]` 在行首）
- 修复日期格式变更后光标同步失效的问题（改用多格式解析）
- 修复一键迁移时丢失冒号后内容的问题（正则只替换日期+星期部分）
- 修复模式切换时补全/裁剪不完整的问题
- 修复全年→每天模式切换后周标题残留的问题
- 修复"今日"按钮未清除其他日期选中状态的问题

---

## [1.0.3] - 2026-06-10

### 新增功能

- **日历视图**：新增右侧日历视图，可直观查看日期并记录
- **今日按钮**：新增"今日"按钮，快速跳转到今天并高亮显示
- **日期导航**：点击日历中的日期，自动打开对应日期的工作记录
- **日期圆点标记**：有工作记录的日期下方显示圆点标记

### 优化改进

- **日期结构优化**：按周分组日期，结构更清晰
- **移动端适配**：优化移动端日历显示效果
- **交互优化**：改进日期点击交互逻辑

### 修复问题

- 修复若干平台兼容性问题
- 修复日期文件生成逻辑错误
- 修复 Win 端日历只显示"1"的显示问题

---

## [1.0.2] - 2026-06-10

### 优化改进

- **日期结构优化**：按周分组日期，结构更清晰
- **移动端适配**：优化移动端日历显示效果

### 修复问题

- 修复若干平台兼容性问题

---

## [1.0.1] - 2026-06-09

### 修复问题

- **修复 Win 端日历显示问题**：修复 Windows 桌面端日历只显示"1"的 bug（修正 `this.isMobile()` 为 `this.app.isMobile`）
- **修复日期文件生成**：修复日期文件生成逻辑，确保正确创建日期标题

---

## [1.0.0] - 2026-06-09

### 首次发布

- **年度工作日志**：支持以年度单文件形式记录每日工作（如 `2026.md`）
- **自动日期结构**：支持自动生成年度日期结构，可选择生成全年或只生成到今天
- **按月分组**：日期按月份分组，结构清晰
- **按周归类**：同一周的日期归类在一起，便于查看
- **日历视图**：提供日历视图，快速跳转至指定日期
- **自定义路径**：支持自定义工作日志文件存放路径
- **Weekly 标题**：自动添加周标题（如 `### Weekly 2026-W01`），便于按周查看
